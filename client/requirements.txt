opencv-python==4.10.0.84
requests==2.32.3
# numpy 2.1.x는 Python 3.10+에서만 제공되는 경우가 많습니다.
# 로컬 파이썬 버전에 따라 설치 가능 버전이 달라지므로 조건부로 고정합니다.
numpy==2.1.3; python_version >= "3.10"
numpy==2.0.2; python_version < "3.10"

